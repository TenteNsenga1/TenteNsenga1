# Optional: enables AI-generated (instead of template-filled) cover letters.
# Get a key at https://console.anthropic.com/ — leave blank to use the plain
# template generator, which works fully offline with no key required.
ANTHROPIC_API_KEY=
